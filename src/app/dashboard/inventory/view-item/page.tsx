import ViewInventoryItem from "@/src/components/dashboard/inventory/ViewInventoryItem";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "View Item",
};

export default ViewInventoryItem;
