import ViewSection from "@/src/components/dashboard/sections/ViewSection";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "View Section",
};

export default ViewSection;
